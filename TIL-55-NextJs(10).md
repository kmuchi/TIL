webkitGetAsEntry()의 활용: 일반적인 파일 업로드(e.dataTransfer.files)는 폴더 구조를 무시하고 알맹이 파일만 가져옵니다. 하지만 items[i].webkitGetAsEntry()를 사용하면 드롭한 것이 '파일'인지 '폴더'인지 식별할 수 있는 객체를 반환합니다.

재귀 탐색 (getFilesFromEntry 함수): 폴더 안에는 또 다른 폴더가 있을 수 있습니다. 이 함수는 폴더를 만나면 그 안을 까보고, 또 폴더가 있으면 다시 까보는 식(재귀)으로 끝까지 파고들어 파일만 건져냅니다.

경로 누적 (currentPath): 재귀적으로 파고들 때마다 현재 폴더의 이름을 currentPath에 더해줍니다. 덕분에 최종 파일에 도달했을 때 최상위폴더/중간폴더/파일명.mp3 형식의 고유한 전체 경로(fullPath)를 저장할 수 있습니다.