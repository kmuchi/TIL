26.02.02

```jsx
docker desktop 키기
docker start protalk_db protalk_redis
```

### JWT 설정

- 비밀키(Secret Key) + 현재시간 + 사용자 정보

### Spring Mail

> 스프링부트에서 이메일 보낼 수 있게 해주는 기능
> 

### Feature-Sliced Design(FSD)

> 비지니스 로직(기능)을 기준으로 코드를 나눔
> 

### UI - API 분리

1. API만
2. model
3. UI만

### 상태 관리 라이브러리’

> 프론트엔드 전역변수 보관소
> 

 없으면 부모 컴포넌트 → 자식 컴포넌트 props 유저 정보 계속 넘겨줘야 함 (프롭 드릴링)

**[상태 관리 라이브러리가 있다면?]**

1. 로그인 정보를 **'Store(보관소)'**라는 곳에 딱 한 번 저장합니다.
2. Header든, MyPage든, PostDetail이든 **필요한 컴포넌트가 Store에 직접 빨대를 꽂아서** 데이터를 가져다 씁니다.
3. 중간에 거쳐 가는 컴포넌트들은 신경 쓸 필요가 없어집니다.
- Zustand
- 전역에서 필요한 useState 같은것 (로그인 여부) → store에서 관리

### React Query

> React Query는 서버 상태 관리를 위한 라이브러리
> 
- API 데이터를 가져오고, 캐싱하고, 동기화하는 걸 자동으로 처리

### 햄버거메뉴(드로어)

> 사이드바 같은 컴포넌트 → 각 컴포넌트에서 반응형으로 만들고 → 조립
> 

## **1️⃣ Tailwind CSS 기본 브레이크포인트 (가장 많이 사용)**

| **접두사** | **min-width** | **대상 기기** |
| --- | --- | --- |
| `sm` | 640px | 큰 모바일, 작은 태블릿 |
| `md` | 768px | 태블릿 (세로) |
| `lg` | 1024px | 태블릿 (가로), 작은 노트북 |
| `xl` | 1280px | 노트북, 데스크탑 |
| `2xl` | 1536px | 큰 모니터 |
|  |  |  |

## **2️⃣ 실무에서 자주 쓰는 분기점**

| **구분** | **너비** | **설명** |
| --- | --- | --- |
| 📱 **모바일** | < 640px | 1열 레이아웃, 햄버거 메뉴 |
| 📱 **태블릿** | 640px ~ 1024px | 2열 가능, 접힌 사이드바 |
| 💻 **데스크탑** | 1024px+ | 전체 레이아웃, 고정 사이드바 |

## **3️⃣ 분기점 선택 기준**

실제 분기점은 **콘텐츠 기반**으로 정하는 게 좋아요:

```

❌ "아이폰은 375px니까 375px에서 바꾸자"
✅ "이 레이아웃이 깨지기 시작하는 지점(예: 800px)에서 바꾸자"
```

**팁:**

1. 브라우저 창을 줄이면서 **레이아웃이 이상해지는 지점** 찾기
2. 그 지점 근처의 Tailwind 브레이크포인트 사용
3. 대부분  하나면 충분 (모바일/태블릿 vs 데스크탑)
    
    ```
    lg (1024px)
    ```
    

**깃브랜치**
`git clone -b {브랜치명} --single-branch {레포지토리_주소}`

```
backend - build.gradle -> dependencies
// .env 파일 로딩
implementation 'me.paulschwarz:spring-dotenv:4.0.0'
```

| **구분** | **docker-compose.yml (기본/배포용)** | **docker-compose.dev.yml (개발용)** |
| --- | --- | --- |
| **코드 반영** | 코드를 이미지로 구워버림 (수정해도 반영 안 됨) | **볼륨(Volume) 연결** (코드 저장하면 바로 화면 바뀜!) |
| **포트** | 보안상 꼭 필요한 포트만 엶 (80, 443) | 디버깅용 포트까지 다 열어둠 (3000, 8080, 9229 등) |
| **로그** | 간결하게 쌓음 | 에러가 났을 때 아주 상세하게 보여줌 |
| **명령어** | `npm start` (최적화된 실행) | `npm run dev` (개발 모드 실행) |