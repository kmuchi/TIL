### 주요 AI 기술

> 객체탐지, 자연어처리, 추천, 예측, LLM, 생성형AI
> 
- 협업필터링 / 콘텐츠 기반 추천
- 시계열 예측 /  이상탐지 ( 주식시장 예측, 수요예측, 질병 예측, 날씨 예보)
- LLM

---

## CNN

> 이미지/영상 같은 격자형 데이터 특징 자동 학습하는데 최적화된 신경망
> 
- 특징 추출 단계
    - 합성곱 / 풀링 반복해 → 이미지 주요 특징 잡아냄
- 분류 단계
    - 추출된 특징을 바탕으로 최종 판

### 서브 샘플링

- 특정 맵 크기 를 줄여(다운샘플링) 계산 줄이고, 과적합 방지

---

## RNN

> 시간적 흐름 or 순서가 있는 데이터 (시계열,텍스트,음성) 다루기 위해 특화된 모델
> 
- 기억
    - 이전 단계 계산 정보(은닉상태) → 다음 단계 전달 → 과거 정보가 현재의 판단에 영향 미침
- 은닉측 → 다시 입력 → 순환구

### RNN CELL

- RNN의 가장 기본적인 요소

### 활용예시

- 음성인식 , 텍스트 자동 완

---

## GAN Architecture

### 판별자 학습

- D_LOSS 줄이는 방향으로 D_Gradients 사용해 모델 가중치 업데이트

### 생성자 학습

- 원본의 통계적 특성 완벽히 학습
- G_Loss 계산, G_Gradients 통해 생성자 가중치 업데이트

### 예시

- 이미지생성

---

## Transformer

> 기존의 답답했던 순차적 처리방식을 버리고, 어텐션 메커니즘 사용
> 
- 크게 인코더 / 디코더로 나뉨

### Encoder (인코더)

- 입력 문장 문맥 이해 → 벡터로 변환하는 역할
1. 단어 순서 정보 → 위치 인코딩
2. 문맥파악 → 멀티헤드어텐션 계층 작용 (문장 내 모든단어들간의 관계 분석)
3. 정보 통합 → 어텐션얻은 문맥정보 → 원래 단어벡터에 더해짐 → 피드포워드 신경망 거쳐 새로운 정보가 통합됨

### Decoder(디코더)

- 인코더 출력 정보를 바탕으로 새로운 문장 생성 역할
- 디코더는 문장생성할때 (이전 생성 단어로만 생성)
    - 마스크드 멀티헤드 어텐션

> 두번째 어텐션 꼐층에서는 인코더 출력정보 + 디코더 현재 입력정보 연결됨
> 

선형꼐층 + 소프트맥스 함수 거쳐 → 다음 단어 확률 계산 → 가장 높은 확률 가진 단어 출력 → 끝까지 반복

---

## Diffusion Model(확산모델)

- 데이터 점진적 노이즈 추가 → 제거 과정 통해 데이터 생성

> Forward 과정 : 원본데이터 → 단계쩍 노이즈 추가 → 완전한 노이즈로 변환
> 

> Reverse 과정 : 신경망이 현재 노이즈 샘플 입력받아 노이즈를 예측 → 이전 단계 샘플로 복원하는 방법을 학습
> 

> 데이터 생성 : 학습된 Reverse 과정을 통해 랜덤 노이즈에서 시작하여 신경망이 점진적으로 노이즈를 제거하여 고품질 데이터를 생성
> 

### 활용예시

- 이미지/비디오 생성
- 음악생성 (suno AI)

---

## AI개발 라이프 사이클

> 지속적 개선이 필요한 순환형 과정이다.
> 
- 각 과정은 독립적인게 아니라, 긴밀하게 연결된 파이프라인
1. 문제 정의/기획
2. 데이터 수집 전처리
3. 모델 개발 학습 최적화
4. 평가 검증
5. 배포 모니터링
6. 유지보수 재학습

---

## AI가 가져온 역할 분담의 변화

---

## 실습

- Miniforge Prompt
    
    ```jsx
    # 가상 환경 활성화
    conda activate ai_env
    # 가상 환경 비활성화
    conda deactivate
    # 가상 환경 삭제
    conda env remove -n ai_env
    # 가상 환경 목록 조회
    conda env list
    ```
    

---

## PyTorch

- Meta AI연구소에 개발
- 딥러닝 학습 /배포 전 과정 관리
- Tensor 연산 자동 미분 지원

### 깔아서 → vscode→ miniforege 환경→ 파일열기

---

### 구글 코랩

- gpu 설정
- ##timeit -n 1 -r 3
    - 매직메서드, 1회실행 3번 반복

```jsx
##timeit -n 1 -r 3
x = torch.randn((4096,4096), device="cuda")
y = torch.randn((4096,4096), device="cuda")
z = torch.matmul(x,y)
torch.cuda.synchronize()

대형텐서 2개 GPU에서 행렬 곱

```

```jsx
{"name": "치", "age": 25}
```