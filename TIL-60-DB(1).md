### 엔티티 vs 속성 판단
- 따로 목록으로 보거나 필터링하고 싶거나, 거기에 부가 정보를 붙이고 싶으면 테이블, 아니면 컬럼

### 카디널리티 결정 (1:N이냐 N:M이냐)
- "X 하나가 Y를 여럿 가질 수 있나? 반대로 Y 하나가 X를 여럿 가질 수 있나?" 둘 다 yes면 N:M
- N:M -> 연결테이블

### 참조 데이터 vs 이벤트 데이터
- 시간 지나면 바뀌는 참조 데이터
- 값이 절때 안바뀌는 이벤트 데이터

### 불변 원장(immutable ledger)
- 틀렸으면 원본을 고치는게 아니라 수정용 거래를 한 줄 새로 추가
- 거래 테이블은 쌓기만 하고 안 건드린다
- UPDATE, DELETE X

### 스냅샷 백업
- 사고가 났을 때 데이터베이스 전체를 어느 시점 상태로 되돌리기 위해 통째로 떠놓는 복사본
- 서버리스 쓰면 자동으로 저장되는 중

### 소프트 삭제
- 기본값은 Sofe Delete
- 테이블에 deleted_at 컬럼 -> 시각을 찍는 걸로 처리
- 조회 쿼리에 WHERE deleted_at IS NULL을 붙여서 화면에서만 안 보이게

### 하드 삭제 
- 법의 보존기간에 따라 보유하고 실제로 삭제
- 탈퇴한 유저의 이메일이 소프트 삭제로 남아 있으면 같은 이메일 재가입이 막히니, 거기엔 별도 처리가 필요
- CASCADE 부모 지우면 -> 자식도 같이
- RESTRICT -> 자식 있으면 삭제 자체를 막음
