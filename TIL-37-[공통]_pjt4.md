### 옵시디언

- 로컬기반 노션이라 보면 됨
- 개인 프로젝트 관리 중점

### 쿠버네티스

- 도커 관리 도구 (현업에서 100% 쓴다.)
- 규모 커지면 쓴다.

**컨테이너가 왜 많아지는가?**

- 스프링 부트 1개 → 3개 (메인 + 부하, 에러방지용)
    - 같은 스프링부트 3개에서도 → 로그인 나누기 / 주문 나누기 / 결제 나누기/ 상품 나누기 하면 → 더 늘어남 15개 가 됨
- db, Nginx(로브밸런서) → 이것도 장애방지용으로 2개씩 띄움
- 로그 수집기 (Fluentd 등) → 각서버마다 1개 or 별도 클러스터
- 메시지 큐 (Kafka, RabbitMQ) → 안정성을 위해 3개를 한세트로
- 모니터링 (Prometheus, Grafana) → 2~3개
- 캐시 (Redis도 죽으면 안되니까 클러스터링 ) → 3개

| **구분** | **현재 (토이/초기)** | **현업 대규모 (MSA/운영)** |
| --- | --- | --- |
| **Backend** | Spring Boot **1개** | 회원(2) + 주문(2) + 결제(2) + 상품(2) = **8개** |
| **Frontend** | React **1개** (Nginx) | 웹(2) + 어드민(1) = **3개** |
| **DB** | MySQL **1개** | 회원DB(2) + 주문DB(2) ... = **6개 이상** |
| **Infra** | Redis(1), Jenkins(1) | Redis클러스터(3), Kafka(3), 모니터링(3), 로그수집(3) = **12개** |
| **총합** | **약 5~6개** | **약 30개 이상** 😱 |


### 포트

> localghost 뒤에 붙는 숫자  (`:8080`, `:3000` 등)
> 
- 하나의 포트는 한번에 하나의 프로그래만 차지

| **프로그램** | **기본 포트** | **설명** | **주의사항** |
| --- | --- | --- | --- |
| **Nginx (웹서버)** | **80** / 443 | 80(HTTP), 443(HTTPS)은 인터넷 기본 포트입니다. | 주소창에 포트 안 적으면 자동으로 80으로 갑니다. |
| **React (프론트)** | **3000** | 리액트 개발 서버 기본값입니다. | Node.js 등도 자주 씁니다. |
| **Spring Boot** | **8080** | 스프링부트 톰캣 기본값입니다. | **젠킨스와 겹쳐서 충돌이 자주 납니다.** |
| **Jenkins** | **8080** | CI/CD 도구 젠킨스 기본값입니다. | **스프링과 동시에 띄우려면 하나를 바꿔야 합니다.** |
| **MySQL** | **3306** | 데이터베이스 기본값입니다. |  |
| **Redis** | **6379** | 메모리 DB 레디스 기본값입니다. |  |

도커쓸때 해결법

YAML

```jsx
ports:
  - "8081:8080"  # (밖 8081 -> 젠킨스 연결)
  - '8080:8080' # (밖 8080 -> 스프링 연결)
```

### Celery

> 파이썬 환경 비동기 작업 큐
> 
- 비동기 작업 큐 = 시간 오래걸리는 일 나중에 처리하는

### Redis

> 데이터 → RAM 저장 초고속 성능 NoSQL 저장소
> 
- 캐싱 작업으로 씀
- 동시접속 100명 +
- 같은 쿼리 반복 (인기글, 랭킹)
- 비동기 작업 필요(Celery)
- 실시간 기능 (채팅,알림)
- next js 자체 캐싱 좋아서 필요없