### RAG vs 추천/검색 시스템

**공통사항**

```jsx
RAG:     문서 → 임베딩 → 벡터DB → 쿼리로 검색 → LLM에 넣어서 답변 생성
추천/검색: 아이템 → 임베딩 → 벡터DB → 쿼리로 검색 → 결과 반환
```

**다른점**

```jsx
RAG에는 없고 추천/검색에만 있는 것들:

- 랭킹 최적화: 10개 결과의 순서가 매출에 직결
- 개인화: 같은 검색어여도 유저마다 다른 결과
- 행동 피드백 루프: 클릭/구매 데이터로 계속 모델 개선
- 다양성 조절: 비슷한 것만 추천하면 유저가 지루해함
- 비즈니스 로직: 재고 있는 것만, 광고 상품 끼워넣기 등
```