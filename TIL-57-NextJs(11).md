### react-i18next

- React 앱에 다국어 기능을 붙이는 라이브러리

### 언어감지

1. 브라우저 언어 (navigator.language)
2. ip 기반 지오로케이션 API
3. 위경도 API → 권한떠서 UX 안좋음 - > 비추
- **우선순위 명확히**: 사용자 설정값(localStorage/cookie) > 브라우저 언어 > IP > 기본값. 사용자가 한 번 바꾸면 그게 항상 이김.
- **첫 페인트에서 결정**: IP 조회는 비동기라 깜빡임(영어→한국어 전환) 생길 수 있음. 그래서 브라우저 언어를 1차로 쓰고 IP는 폴백으로.