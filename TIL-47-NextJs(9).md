낙관적 업데이트
> 서버 응답을 기다리지 않고, 일단 성공했다고 가정하고 화면을 먼저 바꾸는 방식
> 
- 원래: `isLiked = false`, `likeCount = 10`
- 사용자가 클릭
- 화면은 즉시: `isLiked = true`, `likeCount = 11`
- 서버 성공 → 유지
- 서버 실패 → 다시 `false`, `10`으로 롤백

- UI가 훨씬 빠르게 느껴지기 때문에 쓴다.