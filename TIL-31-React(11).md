**🆚 1. iframe vs 컴포넌트 비교표**

| **비교 항목** | **iframe (아이프레임)** | **컴포넌트 (React, Vue 등)** |
| --- | --- | --- |
| **격리성 (Isolation)** | **완벽함.** (부모 페이지와 스타일, 변수 등이 절대 섞이지 않음) | **약함.** (기본적으로 부모의 스타일이나 스크립트의 영향을 받음*) |
| **성능 (Performance)** | **무거움.** (브라우저를 하나 더 띄우는 것과 비슷해 메모리를 많이 먹음) | **가벼움.** (필요한 부분만 렌더링하므로 빠름) |
| **데이터 통신** | **어려움.** (`postMessage`라는 특수 방식으로만 대화 가능) | **쉬움.** (`props`, `state` 등으로 자유롭게 데이터 주고받기 가능) |
| **SEO (검색엔진)** | **나쁨.** (검색 로봇이 iframe 내부를 잘 읽지 못함) | **좋음.** (하나의 페이지로 인식되어 검색에 유리함) |
| **주 사용처** | 유튜브, 지도, 결제창, **완전히 남남인 사이트** 가져올 때 | 내 웹사이트의 메뉴, 버튼, 리스트 등 **UI 구성 요소** |

## 마이크로 프론트엔드(Micro-frontends)란?

- 프론트엔드를 여러 개의 독립된 작은 앱(‘마이크로 앱’)으로 나눠서 개발, 배포, 운영하는 방식
- 백엔드의 마이크로서비스 개념을 프론트에 적용한 것

### 포팅(Porting)

- 소프트웨어를 한 환경에서 다른 환경으로 옮기는 작업
- 로컬 → 배포도

### 스핀박스 (Spinbox) or 넘버 인풋(Number Input)

<input type="number" />

이렇게 `type="number"`만 주면 브라우저가 알아서 오른쪽의 위/아래 화살표(증감 버튼)를 만들어줍니다. 이 화살표들을 **'스핀 버튼(Spin Button)'**이라고 부릅니다.

## HTTP 메서드 정리

| 메서드 | 용도 | 데이터 전송 | fetch 예시 |
| --- | --- | --- | --- |
| **GET** | 조회 (받기) | ❌ URL에만 | `fetch(url)` |
| **POST** | 생성 (보내기) | ✅ body | `fetch(url, {method: 'POST', body: ...})` |
| **PUT** | 수정 (보내기) | ✅ body | `fetch(url, {method: 'PUT', body: ...})` |
| **PATCH** | 부분 수정 | ✅ body | `fetch(url, {method: 'PATCH', body: ...})` |
| **DELETE** | 삭제 | ❌ 보통 없음 | `fetch(url, {method: 'DELETE'})` |

### res

> Response 줄임말 HTTP 응답 객체
>