26-03-04

### 라우트 그룹

> 폴더명을 () 괄호로 감쌈 (movie)
> 
- 단순 폴더정리용 , URL에 포함되지 않음
- ex) (movies) 폴더 → Login/Page.tsx = 그냥 Login 경로로 들가면 나옴 (movies 생략)

### 로딩

- 클라이언트 렌더링 하더라도,
- 조건분기로 → 로딩중이면 서버사이드 렌더링 먼저 보여주고 → 나중에 CSR 로 바꿔치기 (UX 경험 향상)

### 동적 메타데이터

- 그냥 메타데이터는 고정값
- db나 API에서 데이터 가져와서 → 상황에 맞는 메타데이터

**쓰는이유**

- seo최적화
- 카톡/sns 공유시 → 미리보기 바뀜(해당결과에)
- Open Graph 이미지 (OG이미지)
    - sns 공유시 → 큰 미리보기 이미지 동적으로 사용 가능
    - 템플릿으로 짠이미지 → 글자만 바꿔끼워서 동적으로 적용

### ISR

> 이미 만들어진 정적페이지를 배경에서 몰래 업데이트 하는 기술
> 
- **SSG (Static Site Generation):** 빌드할 때 미리 다 만들어둠. (속도는 빠르지만, 내용 수정하려면 사이트 전체를 다시 빌드해야 함 ➔ **매우 번거로움**)
- **SSR (Server Side Rendering):** 사용자 클릭 → 서버에서 만듦 → 사용자에게 전달

⇒ `ISR은 이 둘의 절충안`

**ISR** : (사용자 요청전) 미리 페이지 그려 → 저장(캐시)

- 과정 : 60초마다 페이지 미리 그려둠 → 사용자 접속 → 서버에 저장된 HTML을 즉시 던져줌
- SSR보다 압도적으로 빠름

**ISR 쓰면 안되는 곳**

- 주식/코인거래소 (1초마다 가격 변함)
- 티켓 예매 사이트
- 개인 대시보드 (나만 보는 데이터(로그인필요))

**ISR 쓰면 좋은 곳**

- 개인 블로그나 데이터가 실시간으로 변하지 않는 곳
- 

| **페이지 유형** | **권장 방식** | **이유** |
| --- | --- | --- |
| **랜딩/홍보 페이지** | **ISR** | 속도가 생명이고 SEO가 중요함. 수정이 잦지 않음. |
| **상품 목록/블로그** | **ISR + On-demand** | 기본은 캐시로 빠르게 보여주고, 새 글이 올라올 때만 갱신. |
| **상품 상세 (재고)** | **SSR** | 재고는 1분 전 데이터면 큰일 남. 항상 최신이어야 함. |
| **마이페이지/설정** | **CSR** | 검색 노출 필요 없음. 사용자 개인 데이터라 캐시가 불가능함. |
| **검색 결과 페이지** | **SSR** | 사용자가 뭘 입력할지 모르니 미리 만들 수 없음. |

### Zustand

> 상태 관리 라이브러리 (전역 데이터 관리)
> 
- 예를들어  로그인상태 → 여러페이지에서 써야할때, 한번에 통합 관리 (데이터)

### 미들웨어

> `요청 → 응답 사이에 문지기`
> 

why 미들웨어?

1. 권한 체크 : 로그인 했어?
2. 로그 기록 : 누가,언제 어떤페이지에 왔는지 기록
3. 리다이렉트
4. 데이터 변환

| **구분** | **일반 백엔드 체크** | **Next.js 미들웨어** |
| --- | --- | --- |
| **실행 시점** | 서버 끝단까지 도달한 후 | **서버 입구(Edge)에서 즉시** |
| **속도** | 상대적으로 느림 (DB 연동 등) | **매우 빠름 (경량화된 환경)** |
| **비용** | 서버 자원을 사용함 | **서버 부하를 원천 차단** |

### 현업에서 미들웨어를 쓰는 3가지 시나리오

1. **A/B 테스팅:** 사용자 50%는 A 페이지로, 나머지 50%는 B 페이지로 보내서 어떤 게 더 인기 있는지 테스트할 때.
2. **국가별 언어 설정:** 사용자의 IP를 보고 한국인이면 `/ko`, 미국인이면 `/en` 주소로 자동 연결해줄 때.
3. **봇 차단:** 이상한 요청을 반복하는 나쁜 매크로가 들어오면 아예 입구에서 컷(Cut)할 때.

- GA는 광고 차단 프로그램 쓰면 기록 안됨 → 근데 미들웨어는 서버 발을 들이는 순간 무조건 기록 됨 (정확함)
    - 근데 구체적인 상호작용(브라우저상의) → 이건 GA에 기록됨 (서버에서는 모름)
    - 미들웨어 로그는 = 우리 서버 관리용

### API Routes (`app/api/` 폴더)

> 외부에서 호출하는 엔드포인트
> 

| **구분** | **어디에 담나?** | **누가 관리하나?** | **Next.js + Spring 추천도** |
| --- | --- | --- | --- |
| **바디 (Body)** | HTTP 응답 데이터 안 | 프런트엔드 개발자가 직접 저장/추출 | 보통 (로그인 데이터 전송 시 사용) |
| **헤더 (Header)** | HTTP 요청/응답 헤더 | 프런트엔드 개발자가 코드로 삽입 | 보통 (API 요청 시 사용) |
| **쿠키 (Cookie)** | 브라우저 전용 보관소 | **브라우저가 자동 관리** | **매우 높음 (인증 유지용)** |

| **구분** | **쿠키 세션 방식 (Session)** | **토큰 방식 (JWT)** |
| --- | --- | --- |
| **상태 저장소** | **서버 메모리나 DB**에 세션 정보를 저장 | **클라이언트**가 토큰을 직접 저장 |
| **서버 부담** | 사용자가 많아질수록 서버 부담 증가 | 서버는 검증만 하면 되므로 부담이 적음 |
| **확장성** | 서버가 여러 대면 세션 공유 설정이 복잡함 | 어떤 서버로 요청을 보내도 토큰만 있으면 확인 가능 (**매우 유리**) |
| **만료 제어** | 서버에서 즉시 세션 삭제 가능 (강제 로그아웃) | 토큰이 탈취되면 만료 전까지 제어가 어려움 |

### LocalStorage

> 브라우저 안에만 있는 저장소
> 
- **특징:** 자바스크립트로만 읽고 쓰기 가능, 브라우저 밖으로 절대 나가지 않음
- SSR : 서버에서 먼저 일어남
- 서버는 LocalStorage 들여다 볼 방법이 없음
- 서버는 로그인이 안된 줄 알고 로그인 안된 HTML 보냄 → 브라우저 : 이제 → 화면 바꿈
    - 깜빡임 현상(Layout Shift) : 분명 로그인 했는데, 0.5초 동안 로그인 버튼 보였다가 → 내 프로필로 바뀜
    - 이럼 SSR 쓰는 의미(빠른 로딩, SEO)의미가 퇴색됨
    
    **쿠키**
    
    - 사용자 : 메인 페이지 보여줘 → 요청 보낼때 쿠키주머니 있는 토큰을 자동으로 서버에 보냄

### httpOnly

> 이 쿠키는 JS로 접근 금지
> 
1. XSS 공격 (Cross-Site-Scripting)
    - 해커가 우리 사이트에 몰래 악성 자바스크립트를 심어두는 공격
    - 게시판에 글을 올릴 때  <script> ., 내 토큰을 서버로 보내라 </script> 같은 코드 삽입같은거

| **구분** | **LocalStorage** | **쿠키 (httpOnly)** |
| --- | --- | --- |
| **JS 접근 여부** | 가능 (매우 자유로움) | **불가능** (브라우저만 관리) |
| **XSS 공격** | **탈취 위험 높음** | **탈취 불가능** |
| **주요 용도** | 테마 설정, 비로그인 장바구니 등 | **로그인 토큰, 세션 ID 등 보안 정보** |