### TIL

- 반응형
    - ref() 로 감싸진 변수들 바꾸고 싶다면, 해당변수.value로 unwrapping 해서 써야 한다.
- 인스턴스 객체 할당해서 쓴다. const app = createApp(
- 구조분해할당
    - const { createApp, ref } = Vue
    - Vue 공구함에서, createApp, ref 라는 도구를 꺼내서 책상위에 올려 놓는 느낌이다.
    - 만약에 안쓰면, Vue.createApp(...) 이런식으로 매번 써야함
- const로 쓰고 .value로 내용물만 바꾼다.
- 배열같은경우, html단에서 {{student[0] }} 인덱스로 접근
- sum 구할때 redue함수 사용한다
    
    ```jsx
    const total = scores.value.reduce((acc,cur) => acc+cur,0)
    avgScore.value = total / scores.value.length
    
    ```
    
- 브라우저는 html,css,javascript만 해석이 가능하다.
- vue를 cdn 방식으로 쓰면 ,브라우저에서 vue 통역사를 불러와, 해당 코드를 매번 js로 변환
- npm방식은 js로 변환해서 브라우저로 보냄 (그래서 더 빠름)

- js 문법 사용 가능 slice 같은거 {{ my_info.birth.slice(0, 4) }}